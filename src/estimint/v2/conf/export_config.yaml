predictor: "prev_y9"
target: "eir"
name: "{predictor}-{target}"

# Data
features_scaler_file: train_outputs/features_scaler.pkl
target_scaler_file: train_outputs/target_scaler.pkl


# Model - ensure these match the checkpointed model's parameters
width: 256
depth: 2
mlp_residual: true
dropout_rate: 0.0
n_bins: 32
rqs_bounds: 6

# Checkpointing
checkpoint_dir: ???

# General
seed: 42
artifact_dir: "artifacts/${target}"